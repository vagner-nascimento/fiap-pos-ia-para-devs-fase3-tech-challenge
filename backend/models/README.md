# Models

Este diretorio e reservado para armazenar o modelo gerado pelo processo de fine-tuning.

TODO: ajustar a extensao depois
Quando o treinamento produzir um novo artefato, ele deve ser salvo aqui com o nome `hospital_helper.<extensao>` para ficar disponivel nas proximas execucoes. Se um modelo ja existir neste local, ele pode ser reutilizado diretamente, evitando recriacao e novo treinamento desnecessario.
